<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xamarinForms="clr-namespace:XamarinForms;assembly=XamarinForms"
             xmlns:viewModels="clr-namespace:XamarinForms.ViewModels;assembly=XamarinForms"
             mc:Ignorable="d"
             x:Class="XamarinForms.Views.PredstavePage"
             Title="Predstave">

    <ContentPage.Content>
        <StackLayout>

            <Button Clicked="Search" Text="Pretraga i filtriranje"/>

            <CollectionView ItemsSource="{Binding PredstavaList}" Margin="5,5,0,0">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical"
                        Span="100"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,5,5">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="240" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                            </Grid.ColumnDefinitions>

                            <Image Grid.Column="0" 
                               Grid.Row="0" 
                               Source="slika.png" 
                               Aspect="AspectFill" />
                            <Image Grid.Column="0" 
                               Grid.Row="0" 
                               Source="gradient.png" 
                               Aspect="Fill" />
                            <StackLayout Grid.Column="0" 
                                     Grid.Row="0" 
                                     VerticalOptions="End"
                                     Margin="0,0,0,10" 
                                     Padding="5,0,5,0">
                                <Frame CornerRadius="7"
                                   BackgroundColor="White"
                                   Padding="0,0,0,0"
                                   Margin="45,0,45,0">
                                    <Label Text="{Binding Ocjena, StringFormat='{0:★0.00}'} " 
                                       TextColor="Black" 
                                       HorizontalOptions="CenterAndExpand"/>
                                </Frame>

                                <Label Text="{Binding Naziv}" 
                                   LineBreakMode="TailTruncation" 
                                   TextColor="White" 
                                   FontAttributes="Bold" 
                                   FontSize="Small" 
                                   HorizontalTextAlignment="Center"
                                   VerticalTextAlignment="End"/>
                            </StackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>